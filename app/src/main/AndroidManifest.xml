<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RemoteTap">

        <!-- Entry point: routes to ntfy setup → role selection → server or client -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- First-time setup: enter ntfy server URL and access token -->
        <activity android:name=".ui.NtfySetupActivity" android:exported="false" />

        <!-- Server UI: button recording setup and service status -->
        <activity android:name=".ui.ServerActivity" android:exported="false" />

        <!-- Client UI: the single "Press Button" screen -->
        <activity android:name=".ui.ClientActivity" android:exported="false" />

        <!-- Pairing screen shared by both roles -->
        <activity android:name=".ui.PairingActivity" android:exported="false" />

        <!-- Button recording setup, server only -->
        <activity android:name=".ui.ButtonRecordingActivity" android:exported="false" />

        <!--
            Accessibility service running on Phone A (server).
            Listens for ntfy commands and simulates taps using the recorded button config.
        -->
        <service
            android:name=".service.RemoteTapAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Foreground service that maintains the ntfy streaming connection on Phone A -->
        <service
            android:name=".service.CommandListenerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Restart listener service on boot -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
